apiVersion: database.stacc.com/v1alpha1
kind: DatabaseServer
metadata:
  name: mysql-server
spec:
  type: mysql
  secretName: mysql-server-secret
  mysql:
    host: localhost
    username: my
    port: 3306
    useSsl: false
