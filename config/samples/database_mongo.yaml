apiVersion: database.stacc.com/v1alpha1
kind: Database
metadata:
  name: mongo-db
spec:
  name: mongo-db
  reclaimPolicy: delete
  server:
    name: mongo-server
    namespace: default
  secret:
    name: mongo-db-secret
    namespace: default