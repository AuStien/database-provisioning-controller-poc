apiVersion: database.stacc.com/v1alpha1
kind: DatabaseServer
metadata:
  name: postgres-server
spec:
  type: postgres
  secret:
    name: postgres-server-secret
    namespace: default
  postgres:
    host: localhost
    username: postgres
    port: 5432
    sslmode: require
