
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: databases.database.stacc.com
spec:
  group: database.stacc.com
  names:
    kind: Database
    listKind: DatabaseList
    plural: databases
    singular: database
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Database is the Schema for the databases API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: DatabaseSpec defines the desired state of Database
          properties:
            databaseServerName:
              description: DatabaseServerName is the name of databaseServer on which
                this database is to be created
              type: string
            databaseServerNamespace:
              description: DatabaseServerNamespace is the namespace the databaseServer
                is stored on
              type: string
            name:
              description: Name is the name of the database
              type: string
            secretName:
              description: SecretName is the name of the secret that will be associated
                with the database
              type: string
            secretNamespace:
              description: SecretNamespace is the namespace where the secret will
                be stored
              type: string
            username:
              description: Username is the username to be assigned to the database
                (default is name of database)
              type: string
          type: object
        status:
          description: DatabaseStatus defines the observed state of Database
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
